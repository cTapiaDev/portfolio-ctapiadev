---
interface Props {
    src: string;
    alt?: string;
}

const {src, alt} = Astro.props;
---

<img src=`${src}` alt=`${alt}` class="size-full object-cover grayscale hover:grayscale-0" />

<style>
    img {
        transition: all .5s;
    }
</style>